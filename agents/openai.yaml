interface:
  display_name: "/xxd-panel-052"
  short_description: "Create Panel 052 from its original style brief."
  default_prompt: "Use $xxd-panel-052; treat its bundled original style brief as the sole aesthetic authority, and ask only for unresolved mode, size, text mode, locale, or wallpaper settings."

policy:
  allow_implicit_invocation: true
